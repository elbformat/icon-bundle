{#
    @var icon ?string
    @var iconset string
#}
{% if icon is not null %}
    <i class="icon-{{ icon }}"></i>
    {% if "set2" == iconset %}
        <img src="assets/build/images/icons/{{ icon }}" />
    {% endif %}
{% endif %}